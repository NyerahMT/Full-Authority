name: One-shot Stage 011 exhaust alignment

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/one-shot-stage011-alignment.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Keep plume lip attached under dynamic scaling
        run: python3 .github/patch_stage011_alignment.py

      - name: Commit directly to main
        run: |
          git config user.name "NyerahMT"
          git config user.email "68287092+NyerahMT@users.noreply.github.com"
          git rm .github/patch_stage011_alignment.py .github/workflows/one-shot-stage011-alignment.yml
          git add Aircraft/PrototypeAircraftFactory.swift App/PrototypeSceneView.swift
          git diff --cached --check
          git commit -m "Stage 011.1: lock exhaust plume to nozzle lip"
          git push origin HEAD:main
